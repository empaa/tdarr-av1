# Constraints

Read this before editing the Dockerfile or changing any component version.

This file is populated as decisions are made during development. Each entry records what the constraint is and why it exists — so the reason is never lost.

---

<!-- Constraints will be added here as the project is built. Example format:

## ComponentName vX.Y.Z

**Constraint:** Must use exactly this version / must not exceed this version / etc.

**Why:** Reason this constraint exists (API change, incompatibility, upstream bug, etc.)

-->
